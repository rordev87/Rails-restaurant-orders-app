<div class="container">
  <div class="row">
    <div class="col-xs-6">
      <%= form_for @order, html: { multipart: true } do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Order_for %><br>
    <%= f.text_field :meal, class: "form-control", placeholder: "Enter the meal here"%>
  </div>
  <div class="field">
    <%= f.label :From %><br>
    <%= f.text_field :restaurant, class: "form-control", placeholder: "Enter The restaurant here" %>
  </div>

  <div class="field">
    <%= f.label :Menu %><br>
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit "Add order", class: "btn btn-success" %>
  </div>
<% end %>
    </div>
    <div class="col-xs-6">
    <br/>
      <div class="panel panel-default">
        <div class="panel-body">
          
        </div>
      </div>
    </div>
  </div>
</div>



